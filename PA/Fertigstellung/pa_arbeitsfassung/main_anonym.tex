% Wrapper für die anonymisierte Fassung (ohne Name/Matrikel, ohne PlagAware-Seite)
% Build: latexmk -pdf -jobname=pa_anonym -interaction=nonstopmode -outdir=build main_anonym.tex
\def\ANONYMVERSION{1}
\input{main.tex}
